export const defaults={queue:['1','2','3'],repeat:2,bpm:75,division:16,speed:30,slope:16,wingAngle:33,keys:['ShiftLeft','KeyA','KeyS','KeyD','KeyF','Space'],windows:[{end:35,name:'PERFECT'},{end:80,name:'GREAT'},{end:140,name:'GOOD'}],offset:0,sound:true,hitSound:true,metronome:false,volume:.3};
export function parseUnit(unit){if(!/^(?:[1-6]|\([1-6]+\))+$/.test(unit))throw Error('无效谱面数字串');return unit.match(/\([1-6]+\)|[1-6]/g).map(token=>[...new Set(token.replace(/[()]/g,'').split('').map(n=>Number(n)-1))]);}
export function pitch(lane,shift,mode='diatonic'){const scale=[0,2,4,5,7,9,11];const degree=lane+(mode==='whole'?0:shift);return 60+scale[((degree%7)+7)%7]+12*Math.floor(degree/7)+(mode==='whole'?shift*2:0);}
export function makeChart(data,config){const step=60000/config.bpm*4/config.division;let time=2500;const notes=[];const sections=[];for(const id of config.queue){for(const direction of ['up','down']){for(let r=0;r<config.repeat;r++){const shift=direction==='up'?r:config.repeat-1-r;sections.push({time,id,direction,repeat:r+1,shift});for(const lanes of parseUnit(data[id][direction])){for(const lane of lanes)notes.push({time,lane,midi:pitch(lane,shift,config.transpose),id,direction,repeat:r+1,done:false});time+=step;}}}}return {notes,sections,step,end:notes.length?time-step+Math.max(1000,config.windows.at(-1).end+100):0};}
export function judge(error,windows){return windows.findIndex(w=>Math.abs(error)<=w.end);}
